# `@andekaizat/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @andekaizat/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@andekaizat/prettier-config"
}
```
